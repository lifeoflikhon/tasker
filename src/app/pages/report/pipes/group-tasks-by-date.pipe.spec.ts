import { GroupTasksByDatePipe } from './group-tasks-by-date.pipe';

describe('GroupTasksByDatePipe', () => {
  it('create an instance', () => {
    const pipe = new GroupTasksByDatePipe();
    expect(pipe).toBeTruthy();
  });
});
